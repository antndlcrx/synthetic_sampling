.. highlight:: shell

============
Installation
============


Stable release
--------------

To install synthetic_sampling, run this command in your terminal:

.. code-block:: console

    $ pip install synthetic_sampling

This is the preferred method to install synthetic_sampling, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for synthetic_sampling can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/iboytsov/synthetic_sampling

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/iboytsov/synthetic_sampling/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/iboytsov/synthetic_sampling
.. _tarball: https://github.com/iboytsov/synthetic_sampling/tarball/master
